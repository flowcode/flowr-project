{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'Estimation'}, 'Estimation') }} - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>{{ 'Estimation' | trans({}, 'Estimation')}}</h1>
        <ol class="breadcrumb">
            <li><a href={{ path('dashboard') }}><i class="fa fa-dashboard"></i> {{"Dashboard" | trans({}, 'Estimation')}}</a></li>
            <li><a href="{{ path('estimation') }}"> {{ '%entity% List'|trans({'%entity%': 'Estimation'}, 'Estimation') }}</a></li>
            <li class="active" >{{ 'New %entity%'|trans({'%entity%': 'Estimation'}, 'Estimation') }}</li>
        </ol>
    </section>        
    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{ '%entity% creation'|trans({'%entity%': 'Estimation'}, 'Estimation') }}</h3>
            </div>
            <div class="box-body">
                {{ form_start(form, {action: path('estimation_create')}) }}
                    {{ form_widget(form) }}
                    <p>
                        <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'Estimation') }}</button>
                    </p>
                {{ form_end(form) }}
            </div>
            <div class="box-footer">
                        
        <div class="btn-group record_actions">


        </div>

            </div>
        </div>
    </section>
{% endblock %}
