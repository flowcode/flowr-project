{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'EstimationItem'}, 'EstimationItem') }} - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>{{ 'EstimationItem' | trans({}, 'EstimationItem')}}</h1>
        <ol class="breadcrumb">
            <li><a href={{ path('dashboard') }}><i class="fa fa-dashboard"></i> {{"Dashboard" | trans({}, 'EstimationItem')}}</a></li>
            <li><a href="{{ path('estimationitem') }}"> {{ '%entity% List'|trans({'%entity%': 'EstimationItem'}, 'EstimationItem') }}</a></li>
            <li class="active" >{{ 'New %entity%'|trans({'%entity%': 'EstimationItem'}, 'EstimationItem') }}</li>
        </ol>
    </section>        
    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{ '%entity% creation'|trans({'%entity%': 'EstimationItem'}, 'EstimationItem') }}</h3>
            </div>
            <div class="box-body">
                {{ form_start(form, {action: path('estimationitem_create')}) }}
                    {{ form_widget(form) }}
                    <p>
                        <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'EstimationItem') }}</button>
                    </p>
                {{ form_end(form) }}
            </div>
            <div class="box-footer">
                        
        <div class="btn-group record_actions">


        </div>

            </div>
        </div>
    </section>
{% endblock %}
