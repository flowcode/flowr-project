{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'Project'}, 'Project') }} - {{ parent() }}{% endblock %}

{% block body %}
    {{ include("FlowerProjectBundle:Project:header.html.twig", { 'project': project }) }}      
    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{ '%entity% creation'|trans({'%entity%': 'Project'}, 'Project') }}</h3>
            </div>
            <div class="box-body">
                {{ form_start(form, {action: path('project_create')}) }}
                    {{ form_widget(form) }}
                    <p>
                        <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'Project') }}</button>
                    </p>
                {{ form_end(form) }}
            </div>
            <div class="box-footer">

        <div class="btn-group record_actions">


        </div>

            </div>
        </div>
    </section>
{% endblock %}
