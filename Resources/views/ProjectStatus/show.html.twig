{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}ProjectStatus - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>{{ 'ProjectStatus' | trans({}, 'ProjectStatus')}}</h1>
        <ol class="breadcrumb">
            <li><a href={{ path('dashboard') }}><i class="fa fa-dashboard"></i> {{"Dashboard" | trans({}, 'ProjectStatus')}}</a></li>
            <li><a href="{{ path('projectstatus') }}"> {{ '%entity% list'|trans({'%entity%': 'ProjectStatus'}, 'ProjectStatus') }}</a></li>
            <li class="active" >{{ '%entity%'|trans({'%entity%': 'ProjectStatus'}, 'ProjectStatus') }}</li>
        </ol>
    </section>
    <section class="content">    
        <div class="box">
            <div class="box-header">
                <h3 class="box-title"></h3>                            </div>
            <div class="box-body">
                <table class="table table-hover record_properties">
                    <tbody>
                        <tr>
                            <th scope="row">{{'Id'|trans({}, 'ProjectStatus')}}</th>
                            <td>{{ projectstatus.id }}</td>
                        </tr>
                        <tr>
                            <th scope="row">{{'Name'|trans({}, 'ProjectStatus')}}</th>
                            <td>{{ projectstatus.name }}</td>
                        </tr>
                        <tr>
                            <th scope="row">{{'Description'|trans({}, 'ProjectStatus')}}</th>
                            <td>{{ projectstatus.description }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="box-footer">
            {{ form_start(delete_form) }}
        {{ form_widget(delete_form) }}

        <div class="btn-group record_actions">

            <a class="btn btn-primary" href="{{ path('projectstatus_edit', {id: projectstatus.id}) }}">
                <i class="fa fa-edit"></i> {{ 'Edit'|trans({}, 'ProjectStatus') }}
            </a>
            <button class="btn btn-danger" type="submit"><i class="fa fa-trash-o"></i> {{ 'Delete'|trans({}, 'ProjectStatus') }}</button>

        </div>

        <input name="modal" type="hidden" value="0">
    {{ form_end(delete_form) }}

    <div id="delete_confirm" class="modal fade">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h3>{{ 'Confirm delete'|trans({}, 'ProjectStatus') }}</h3>
                </div>
                <div class="modal-body">
                    <p>{{ 'You are about to delete an item'|trans({}, 'ProjectStatus') }}, {{ 'this procedure is irreversible'|trans({}, 'ProjectStatus') }}.</p>
                    <p>{{ 'Do you want to proceed?'|trans({}, 'ProjectStatus') }}</p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-danger">{{ 'Yes'|trans({}, 'ProjectStatus') }}</button>
                    <button class="btn btn-default" data-dismiss="modal">{{ 'No'|trans({}, 'ProjectStatus') }}</button>
                </div>
            </div>
        </div>
    </div>
</div>
        </div>
    
    </section>

{% endblock %}
