{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'ProjectStatus'}, 'ProjectStatus') }} - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>{{ 'ProjectStatus' | trans({}, 'ProjectStatus')}}</h1>
        <ol class="breadcrumb">
            <li><a href={{ path('dashboard') }}><i class="fa fa-dashboard"></i> {{"Dashboard" | trans({}, 'ProjectStatus')}}</a></li>
            <li><a href="{{ path('projectstatus') }}"> {{ '%entity% List'|trans({'%entity%': 'ProjectStatus'}, 'ProjectStatus') }}</a></li>
            <li class="active" >{{ 'New %entity%'|trans({'%entity%': 'ProjectStatus'}, 'ProjectStatus') }}</li>
        </ol>
    </section>        
    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{ '%entity% creation'|trans({'%entity%': 'ProjectStatus'}, 'ProjectStatus') }}</h3>
            </div>
            <div class="box-body">
                {{ form_start(form, {action: path('projectstatus_create')}) }}
                    {{ form_widget(form) }}
                    <p>
                        <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'ProjectStatus') }}</button>
                    </p>
                {{ form_end(form) }}
            </div>
            <div class="box-footer">
                        
        <div class="btn-group record_actions">


        </div>

            </div>
        </div>
    </section>
{% endblock %}
